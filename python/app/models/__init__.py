"""
Models package - all business domain models

Note: Models are NOT auto-imported here to avoid circular import issues.
Tortoise ORM will discover models directly from submodules.
Import specific models directly when needed, e.g.:
    from app.models.system.store import Store
"""
# Empty __init__.py - models are discovered by Tortoise ORM from submodules
# This avoids circular import issues caused by auto-importing all models